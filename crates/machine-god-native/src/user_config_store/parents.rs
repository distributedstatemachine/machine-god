//! Settings regression tests for the shared parent-observation authority.
use super::NativeUserConfigError as Error;
use crate::bounded_profile_file::parents::ParentObservation;
use std::path::{Path, PathBuf};
mod tests;
