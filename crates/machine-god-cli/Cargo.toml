[package]
name = "machine-god-cli"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[[bin]]
name = "machine-god"
path = "src/main.rs"

[dependencies]
futures-core.workspace = true
machine-god-core.workspace = true
machine-god-native.workspace = true

[target.'cfg(not(target_family = "wasm"))'.dependencies]
machine-god-native = { workspace = true, features = ["ai-gateway-model-catalog-http"] }
tokio = { workspace = true, features = ["signal"] }

[target.'cfg(any(target_os = "linux", target_os = "macos"))'.dependencies]
machine-god-native = { workspace = true, features = ["ai-gateway-http"] }

[target.'cfg(not(target_family = "wasm"))'.dev-dependencies]
tokio = { workspace = true, features = ["test-util"] }

[lints]
workspace = true
